# 2026-09-09 知识库历史迁移

迁移目标为 192.168.19.239 的知识库业务库 `yudao_ai`、AI PostgreSQL 的同名库和 `/data/ai/knowledge/documents`。

## 范围和合并规则

- 迁移知识库、目录、文档、切片、用户、相关部门、同步记录、人事原始记录及问答历史。保留软删除状态。
- 文档、知识库、切片、向量和数据源编号保持一致。会话、消息、引用编号增加 1000000，并同步更新关联。
- 原 `admin` 与服务器同名账号合并，保留服务器密码与资料。其他历史用户编号增加 1000000，保留密码哈希、状态及会话归属。
- 48 个历史会话均属于原 `admin`。现有管理员策略允许 `knowledge-admin` 查看同租户历史会话；迁移不改变会话归属。当前登录实现将用户视为管理员，普通员工角色权限仍待完善，不能声明已实现按用户隔离。
- 保留服务器 `knowledge-admin`、Dify 绑定、原有会话、菜单、配置和数据中台记录。
- 历史数据源 `sync_enabled` 设为 0；恢复历史不会自动启动采集。原文件 URI 如为本机 file 路径，则改为服务器存储路径，object_key 不变。
- 不导入旧问答缓存、FastGPT 配置和其他业务配置；完整源数据库备份保留在私有备份目录。
- 当前 Dify 默认问答入口仅选择明确绑定的知识库。迁入历史资料不代表已经导入 Dify。

## 源数据已知缺失

86 条文档记录中，53 份原文件可读取。33 份原对象在 NAS 返回 `NoSuchKey`，本机目录和本机 MinIO 亦未找到；其中 30 份属于未删除文档。迁移保留这些记录和已有切片，不以切片拼接伪造原文件。

缺失清单及文件校验值位于私有备份的 `source/manifest.json`，不将历史正文、用户资料、密码哈希和凭据提交到仓库。
源桶未启用版本历史，本机其他附件中也未发现内容哈希相同的原件。

## 备份与验证

服务器备份目录：`/data/backup/knowledge-history/20260909`。
本机私有备份目录：`C:\Users\admin\.codex\private\knowledge-192.168.19.239\history-20260909`。

先将源备份恢复到隔离库 `knowledge_history_20260909`，将生产备份与合并脚本恢复到候选库 `knowledge_candidate_20260909`。逐字段核对候选记录后才执行生产导入；生产写入期间只停止 `leman-knowledge.service`。

执行脚本：`export-history.py`、`recover-history-files.py`、`prepare-history-merge.py`、`server-history.py`（backup、stage、rehearse、apply、verify）、`verify-history-api.py`。

后端在隔离构建目录执行 `mvn -B -ntp package`：150 项测试，145 通过、5 项 PostgreSQL 集成测试跳过，构建成功。新增回归测试覆盖“存在历史知识库时，Dify 默认入口仍只选择绑定库”。

执行结果以迁移目录的 `rehearsal.json`、`applied.json`、`api-verification.json` 为准；这些文件未生成前不得声明生产迁移完成。

### 本次执行结果

正式迁移与核验已完成：12 条知识库记录、86 条文档记录、45,294 条切片、48 个会话、152 条消息、579 条引用、38,821 条向量及 53 份原文件。新增 5 条历史用户记录，原 admin 按上述规则合并。

接口核验了全部 39 个未删除历史会话及其 132 条消息；37 份当前可访问附件的预览内容与源文件哈希一致。其他文件包含已删除文档或属于已删除知识库，保留原状态。页面历史列表已实际验证；Dify 默认入口返回答案及引用。

后端版本：`/opt/data-center/apps/knowledge/releases/20260909_history_v1`，JAR SHA-256：`d08c856675a1eda7163130e57a88d34deae7b7852ae903d906e9f68752f641c2`。

服务 active、开机启动 enabled；数据中台 80、Dify 18080、知识库 18081 均返回 HTTP 200，OpenAI 认证检查 HTTP 200。

迁移后 NAS 备份：`dw-backup/host/20260909_094918`（5 个文件）及 `dw-backup/dify/20260909_094918`（9 个文件），远端读回校验均通过。隔离演练库验证后清理，原始备份继续保留。

仍未完成：补齐源端缺失的 33 份原文件、历史资料接入 Dify 检索、普通员工角色权限完善。

## 回退

本机源数据不删除。若正式写入后需要回退，应先停止知识库后端并备份回退时的新数据，再使用迁移前 MySQL、PostgreSQL、文件备份恢复，恢复 `previous-release.txt` 中记录的版本，随后验证并启动服务。不得在存在迁移后新写入时直接覆盖整个业务库。
