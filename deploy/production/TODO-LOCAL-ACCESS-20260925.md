# 本机保存 239 访问凭据与发布流程

- [x] 已完成：在项目本机目录用 Windows DPAPI 加密保存 SSH 凭据、限制 ACL，并排除 Git 跟踪。
- [x] 已完成：提供读取凭据的连接脚本；执行 `python deploy/production/ssh-239.py hostname` 返回 `leman`。
- [x] 已完成：在项目约定中记录本地开发、验证、发布到 239 的顺序。
